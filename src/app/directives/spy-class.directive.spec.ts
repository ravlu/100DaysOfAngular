import { SpyClassDirective } from './spy-class.directive';

describe('SpyClassDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyClassDirective();
    expect(directive).toBeTruthy();
  });
});
